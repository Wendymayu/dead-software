# 六边形架构 (Hexagonal Architecture)

## 什么是六边形架构

六边形架构将**核心业务逻辑放在中心**，通过端口(Port)定义它需要的接口，通过适配器(Adapter)连接外部世界。核心不知道外部的存在——它只声明"我需要存数据、我需要发通知"，具体怎么存、怎么发，由适配器决定。

## 核心思想

**依赖反转**：外部依赖核心，核心不依赖外部。端口是核心的"需求声明"，适配器是外部的"实现提供"。

```
            ┌─────────────┐
  CLI ──────│             │────── 数据库
  (驱动)    │   核心业务   │(被驱动)
            │  ┌─ 端口 ─┐ │
  测试 ─────│  │ 存数据 │ │────── 文件系统
  (驱动)    │  │ 发通知 │ │(被驱动)
            │  └─ 端口 ─┘ │────── 邮件服务
            └─────────────┘(被驱动)
```

关键特征：
- **核心只定义端口** — 声明需要什么能力，不关心谁来实现
- **适配器插拔替换** — 换数据库、换通知方式，核心代码零修改
- **驱动侧与被驱动侧** — CLI/测试是驱动适配器（调用核心），数据库/邮件是被驱动适配器（核心调用它们）

## 代码示例

运行基础示例：

```bash
python example.py
```

关键代码解读：

1. **Repository / NotificationPort** — 端口，抽象接口。核心声明"我需要存数据、我需要发通知"
2. **UserService** — 核心业务逻辑。构造函数接收端口（`repo: Repository`），绝不引用具体适配器
3. **InMemoryRepo / ConsoleNotifier** — 被驱动适配器，为外部系统实现端口
4. **`UserService(InMemoryRepo(), ConsoleNotifier())`** — 适配器从外部注入，核心不知道用了什么实现

核心只说"给我一个能存数据的端口"，至于用内存还是数据库——核心不在乎。这就是依赖反转。

## 进阶示例

运行进阶示例：

```bash
python advanced.py
```

进阶示例展示两个实用特性：

1. **适配器替换** — InMemoryRepo 和 FileRepo 都实现同一个 Repository 端口，切换适配器核心代码零修改
2. **驱动适配器** — `cli_adapter()` 函数从外部调用核心，核心不知道自己是被 CLI 调用还是被测试调用

## 优缺点

**优点**
- 核心业务可独立测试——只需注入模拟适配器
- 适配器可自由替换——换数据库、换消息队列，不改核心
- 新增外部系统不改核心——只需写新适配器
- 清晰边界——端口划定了核心与外部的分界线

**缺点**
- 端口设计需要前瞻性——端口定义不当会导致频繁重构
- 简单场景过度设计——一个 CRUD API 不需要六边形
- 适配器数量可能多——每个外部系统都要写适配器
- 间接层增加复杂度——每个操作都经过端口抽象

## 真实项目中的应用

- **Spring Boot with Hexagonal** — Java 社区最流行的六边形实践，结合 DDD 风格组织代码
- **Amazon Lambda** — 处理函数是核心，触发器（API Gateway/S3/SQS）是驱动适配器
- **现代 DDD 项目** — 六边形架构是 DDD 的标准技术实现方式，核心放领域模型

## 进一步阅读

- Alistair Cockburn — [Hexagonal Architecture](https://alistair.cockburn.us/hexagonal-architecture/)（原始定义）
- 《软件架构模式》 (Mark Richards) — 六边形架构的详细分析
- 《Clean Architecture》 (Robert C. Martin) — 依赖反转原则的深入阐述
