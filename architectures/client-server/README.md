# 客户端-服务器架构 (Client-Server)

## 什么是客户端-服务器架构

客户端-服务器是最基础的分布式模型：客户端发送请求，服务器接收请求、处理后返回响应。服务器是资源持有者和服务提供者，客户端是资源消费者。所有通信都是请求/响应模式。

## 核心思想

**请求/响应 + 中心化服务**：服务器是唯一的服务提供者，客户端只能通过请求获取服务。

```
  Client A  Client B  Client C
     ↓         ↓         ↓
  请求       请求       请求
     ↓         ↓         ↓
          Server
       (资源/服务持有者)
          ↓
      响应各客户端
```

关键特征：
- **服务器是中心** — 拥有数据和处理能力，客户端不持有核心资源
- **请求/响应模式** — 客户端发起，服务器回应，通信总是单向启动
- **多客户端共享** — 多个客户端访问同一服务器，服务器协调共享资源

## 代码示例

运行基础示例：

```bash
python example.py
```

关键代码解读：

1. **Server** — 持有数据（users），提供 handle_request 处理请求并返回响应字典
2. **Client** — 构造请求参数，调用 server.handle_request，接收响应
3. **多客户端共享** — Client A 添加数据后，Client B 能查询到，体现服务器作为共享中心
4. **请求/响应** — 每次交互都是客户端主动发起，服务器被动响应

客户端不持有数据，一切通过请求获取——这是中心化服务的本质。

## 进阶示例

运行进阶示例：

```bash
python advanced.py
```

进阶示例展示更接近真实网络场景：**请求队列 + 回调分发**。

- 请求先排队，服务器统一处理——模拟网络请求到达服务器的时序
- 服务器通过回调将响应分发给对应客户端——模拟异步响应
- 客户端注册回调函数——模拟连接建立后的通知机制

## 优缺点

**优点**
- 简单直观——请求/响应是最自然的交互模型
- 中心化管理——数据一致性由服务器保证，客户端无需协调
- 安全可控——服务器是唯一入口，便于权限控制和安全审计
- 便于扩展客户端——新增客户端不影响服务器逻辑

**缺点**
- 单点故障——服务器宕机则所有客户端不可用
- 性能瓶颈——所有请求集中到服务器，高并发时容易过载
- 网络依赖——客户端必须能连接服务器，断网即失效
- 扩展受限——垂直扩展有限，水平扩展需要额外负载均衡

## 真实项目中的应用

- **HTTP/Web 服务** — 浏览器(Client)请求 Web 服务器(Server)，最经典的实例
- **数据库访问** — 应用程序(Client)连接数据库服务器(Server)查询数据
- **Email 系统** — 邮件客户端发送/接收请求到邮件服务器

## 进一步阅读

- 《Computer Networks》 (Andrew Tanenbaum) — 网络基础，客户端-服务器模型的底层机制
- RFC 2616 — HTTP/1.1 协议规范，最广泛使用的客户端-服务器协议
- 《Designing Data-Intensive Applications》 (Martin Kleppmann) — 分布式系统中的服务器架构演变
