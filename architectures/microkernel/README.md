# 微内核架构 (Microkernel Architecture)

## 什么是微内核架构

微内核架构将系统分为极小的核心和可选的扩展。核心只提供最基本的服务（调度、进程管理、通信），文件系统、网络、驱动等都作为可选扩展运行在核心之外。与插件架构的区别：微内核的核心更薄，扩展提供的是系统基础能力而非锦上添花的功能。

## 核心思想

**最小核心 + 基础能力扩展**：核心只做调度和最基本服务，系统级能力（文件、网络、进程）都是扩展。

```
    ┌── Microkernel Core ──┐
    │  调度 / 通信 / 进程表 │ ← 最薄核心
    └──────────────────────┘
         ↓ 调度       ↑ 结果
  ┌──────┐ ┌──────┐ ┌──────┐
  │ FS   │ │ Net  │ │ Driver│ ← 基础系统能力
  │扩展  │ │扩展  │ │ 扩展  │   (不是"锦上添花")
  └──────┘ └──────┘ └──────┘
```

关键约束：
- **核心极薄** — 只做调度和最基本服务，比插件架构的核心更薄
- **扩展是基础能力** — 文件系统、网络不是"可选功能"，是系统运行的基础能力
- **扩展间通过核心中转** — 扩展不直接通信，核心是唯一中介

## 代码示例

运行基础示例：

```bash
python example.py
```

关键代码解读：

1. **MicrokernelCore** — 极薄核心：register_extension + start + request（只做调度）
2. **FileSystemExtension / ProcessExtension** — 系统基础能力，不是锦上添花的插件
3. **request()** — 核心只做路由调度，具体工作交给扩展
4. **对比插件** — 插件是可选功能（加密、压缩）；微内核扩展是基础能力（文件、进程）

核心比插件架构更薄——调度是核心唯一职责，甚至进程管理在纯微内核中也在核心外。

## 进阶示例

运行进阶示例：

```bash
python advanced.py
```

进阶示例展示微内核的两个核心机制：**核心内进程管理 + 扩展间中转通信**。

- `create_process()` — 进程表在核心内，是最基本的核心能力
- `relay()` — 扩展间通信必须通过核心中转，不直接交互
- fs 读文件 → net 发送 → log 记录，全部通过核心调度

## 优缺点

**优点**
- 核心极简——核心代码少，容易理解和验证正确性
- 灵活组合——不同场景加载不同扩展，定制最小系统
- 扩展独立——扩展故障不影响核心运行，可靠性高
- 适合操作系统——天然匹配"内核+驱动/模块"的组织方式

**缺点**
- 性能开销——扩展间通信必须经过核心中转，频繁调用有性能损失
- 核心边界难定——什么算"最基本"有争议，核心膨胀则违背初衷
- 用户体验碎片——用户需要自己组合扩展，配置复杂
- 调试困难——问题可能在核心、扩展、或核心-扩展接口，定位复杂

## 真实项目中的应用

- **MINIX 3** — 微内核操作系统的经典实现，核心仅约 12000 行
- **seL4** — 形式化验证的微内核，数学证明核心代码无 bug
- **Eclipse IDE** — 核心只提供工作台和扩展点，所有功能都是扩展

## 进一步阅读

- 《Operating Systems: Design and Implementation》 (Tanenbaum) — MINIX 微内核设计
- seL4 项目 — [seL4.org](https://sel4.systems/) 形式化验证微内核
- 《Pattern-Oriented Software Architecture》 (Buschmann) — 微内核模式的系统论述
